version: '3.7'

services:

  example:
    container_name: tictactoe-docker
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - '.:/app'
      - '/app/node_modules'
    ports:
      - '4200'
